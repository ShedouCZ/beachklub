#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-json/bin/grunt-json" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-json/bin/grunt-json" "$@"
  ret=$?
fi
exit $ret
