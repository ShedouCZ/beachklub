#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../po2json/bin/po2json" "$@"
  ret=$?
else 
  node  "$basedir/../po2json/bin/po2json" "$@"
  ret=$?
fi
exit $ret
